import React from 'react'

const page = () => {
    // Home page should have the Name of the App and the tagline
    // It should also include the intention behind the app.
    return (
        <section className="hero_box">
            
        </section>
    )
}

export default page